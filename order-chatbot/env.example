# ============================================
# DATABASE CONFIGURATION
# ============================================
# Sử dụng DB_* cho các module mới (chain.py, services/database.py)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=chatbot_db
DB_PORT=3306

# Sử dụng MYSQL_* cho các module cũ (config/database.py, faq_handler.py)
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=chatbot_db

# ============================================
# GOOGLE GEMINI API CONFIGURATION
# ============================================
# Lấy API key từ: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# SERVER CONFIGURATION
# ============================================
HOST=0.0.0.0
PORT=8000

# ============================================
# NOTES
# ============================================
# 1. Copy file này thành .env và điền thông tin thực tế
# 2. KHÔNG commit file .env lên GitHub (đã có trong .gitignore)
# 3. Đảm bảo database đã được tạo bằng schema.sql trước khi chạy

